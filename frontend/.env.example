# Frontend environment example
# Force showing passkey button even over HTTP (DEV ONLY)
REACT_APP_FORCE_PASSKEY_DEV=0
# Optionally hardcode API base (otherwise dynamic based on window.location)
# REACT_APP_API_BASE=http://localhost:4000
