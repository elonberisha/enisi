# Example environment file (copy to .env and fill real values)
# DO NOT commit real secrets.

NODE_ENV=production
PORT=4000
DEBUG_ERRORS=0

# PostgreSQL (managed: Supabase / Neon / Render)
DATABASE_URL=postgresql://postgres:Loniloni.123@db.civqfftsrgfyhtwaaser.supabase.co:5432/postgres?sslmode=require


# Sessions
SESSION_SECRET=CHANGE_ME_LONG_RANDOM

# CORS (frontend domain only)
CORS_ORIGINS=https://tendaenisi.in

# OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://tendaenisi.in/api/auth/google/callback

# WebAuthn
RP_ID=tendaenisi.in
RP_ORIGIN=https://tendaenisi.in
ALLOW_DYNAMIC_RP=0

# Email (optional)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=
EMAIL_PASS=
